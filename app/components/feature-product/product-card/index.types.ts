export interface IProps {
  item: {
    title: string[];
    buttonText: string;
    bgColor: string;
    imagePath: string;
    image: string;
    // firstColor: string;
    // secondColor: string;
  };
}
